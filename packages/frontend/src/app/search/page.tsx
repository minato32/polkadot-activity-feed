import { SearchPage } from "@/components/search/SearchPage";

export const metadata = {
  title: "Search | Polkadot Activity Feed",
};

export default function SearchRoute() {
  return <SearchPage />;
}
